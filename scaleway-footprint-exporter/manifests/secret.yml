# TEMPLATE — DO NOT COMMIT REAL VALUES
#
# Replace placeholders before applying, OR avoid committing this file with real
# values entirely and create the Secret directly:
#
#   kubectl -n keit create secret generic scaleway-footprint-credentials \
#     --from-literal=SCW_ORG_ID="$SCW_ORG_ID" \
#     --from-literal=SCW_SECRET_KEY="$SCW_SECRET_KEY"
#
# For GitOps deployments, encrypt with SOPS / sealed-secrets / external-secrets
# per your cluster's conventions.
apiVersion: v1
kind: Secret
metadata:
  name: scaleway-footprint-credentials
  namespace: keit
type: Opaque
stringData:
  SCW_ORG_ID: "REPLACE_WITH_SCALEWAY_ORG_UUID"
  SCW_SECRET_KEY: "REPLACE_WITH_API_KEY_SECRET"
